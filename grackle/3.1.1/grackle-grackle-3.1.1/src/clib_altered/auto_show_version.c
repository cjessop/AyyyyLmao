#include <stdio.h>
void auto_show_version(FILE *fp) {
   fprintf (fp,"\n");
   fprintf (fp,"The Grackle Version 3.1.1\n");
   fprintf (fp,"fatal: Not a git repository (or any parent up to mount point /mnt)\n");
   fprintf (fp,"Stopping at filesystem boundary (GIT_DISCOVERY_ACROSS_FILESYSTEM not set).\n");
   fprintf (fp,"Git Branch   \n");
   fprintf (fp,"fatal: Not a git repository (or any parent up to mount point /mnt)\n");
   fprintf (fp,"Stopping at filesystem boundary (GIT_DISCOVERY_ACROSS_FILESYSTEM not set).\n");
   fprintf (fp,"Git Revision \n");
   fprintf (fp,"\n");
}
